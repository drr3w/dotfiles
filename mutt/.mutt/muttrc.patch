diff --git a/.mutt/muttrc b/.mutt/muttrc
index aa87829..d8369ca 100644
--- a/.mutt/muttrc
+++ b/.mutt/muttrc
@@ -1,29 +1,23 @@
-#--------------------------------------------------------------------
-#
-# .muttrc - mutt config file
-#
-#--------------------------------------------------------------------
-
 
-#--------------------------------------------------------------------
+#-------------------------------------------------------
 # Basic settings
-#--------------------------------------------------------------------
+#-------------------------------------------------------
 set ssl_starttls=yes
 set ssl_force_tls=yes
 
 set folder = "imaps://imap.gmail.com:993"   # Paths starting with '+' expand to this
 set spoolfile = "+INBOX"
 set postponed = "+[GMail]/Drafts"
-
-set imap_user = USER
-set imap_pass = PASSWORD
-set smtp_pass = PASSWORD
-set record = ''          # set record blank since we're using google smtp and it saves sent mails
-set from= FROM EMAIL
-set smtp_url = 'smtp://USER@smtp.gmail.com:587'
-set realname= NAME
+set smtp_authenticators = 'gssapi:login'
+set imap_user = 'drew@moj.io'
+set imap_pass = yquwruernqyaprcb 
+set smtp_pass = yquwruernqyaprcb
+set record = ''                             # set record blank since we're using google smtp and it saves sent mails
+set from = 'drew@moj.io"
+set smtp_url = 'smtp://drew@moj.io@smtp.gmail.com:587'
+set realname= 'Andrew Hersh'
 set imap_check_subscribed=yes
-set hostname = gmail.com
+set hostname = moj.io
 set mail_check = 120
 set timeout = 300
 set header_cache = "~/.mutt/cache/headers"
@@ -32,19 +26,28 @@ set certificate_file = "~/.mutt/certificates"
 set move = no
 set imap_keepalive = 900
 
-#--------------------------------------------------------------------
+#-------------------------------------------------------
 # My custom  settings
-#--------------------------------------------------------------------
+#-------------------------------------------------------
 
 source ~/.mutt/zenburn.colors
-set signature = ~/.mutt/signature.sh|
-set sleep_time = 0                          # Be fast. That's it.
-bind pager <up> previous-line               # scroll inside the message rather than the index
-bind pager <down> next-line
-set smart_wrap
+set date_format="%a %b %e %k:%M"
+set index_format="%2C | %Z | %D %-30.30F (%-4.4c) %s"
+set sleep_time = 0                          # Be fast. Th
+set smart_wrap 
+set editor = `which nvim`
+set signature = "~/.mutt/signature"
 set sort = threads
+#set sort_aux = reverse-date-received
 set sort_aux = reverse-last-date-received
-bind editor <space> noop
+
+bind pager <up> previous-line               #scroll insid
+bind pager <down> next-line
+bind editor <space> noop   
+bind index,pager a group-reply
+bind index q noop
+bind index Q quit
+
 macro index,pager y "<save-message>=[Gmail]/All Mail<enter><enter>" "Archive"
 macro index gi "<change-folder>=INBOX<enter>" "Go to inbox"
 macro index ga "<change-folder>=[Gmail]/All Mail<enter>" "Go to all mail"
@@ -53,31 +56,20 @@ macro index gd "<change-folder>=[Gmail]/Drafts<enter>" "Go to drafts"
 macro pager u <pipe-entry>"urlview<enter>"  "Use urlview to view links in email"
 
 #-------------------------------------------------------
-# vim-ish movement
-#-------------------------------------------------------
-bind index k previous-entry
-bind index j next-entry
-bind pager k previous-line
-bind pager j next-line
-
-#--------------------------------------------------------------------
 # Sidebar stuff
-#
-# All this stuff requires that mutt have the sidebar patch installed.
-#
-#--------------------------------------------------------------------
+#-------------------------------------------------------
 set sidebar_visible = no
 set sidebar_width = 30
 macro index,pager \cb "<enter-command>toggle sidebar_visible<enter>"
 bind index,pager \cn sidebar-next
 bind index,pager \cp sidebar-prev
 bind index,pager \co sidebar-open
-mailboxes +INBOX +[Gmail]/"All Mail" +[Gmail]/Drafts +[Gmail]/"Sent Mail" +[Gmail]/Starred  +[Gmail]/Important +[Gmail]/Spam +[Gmail]/Trash
+mailboxes +INBOX +[Gmail]/"All Mail" +[Gmail]/Drafts +[Gmail]/"Sent Mail" +[Gmail]/Starred  +[Gmail]/Important +[Gmail]/Trash
 
 #--------------------------------------------------------------------
 # google contacts
 #--------------------------------------------------------------------
 
 set query_command="goobook query %s"
-macro index,pager a "<pipe-message>goobook add<return>" "add sender to google contacts"
+#macro index,pager a "<pipe-message>goobook add<return>" "add sender to google contacts"
 bind editor <Tab> complete-query
